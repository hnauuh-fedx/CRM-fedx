# Temporary local credential for Metabase content automation.
# Create this API key in Metabase Admin and revoke it after setup is complete.
METABASE_API_KEY=replace-with-a-temporary-metabase-api-key
